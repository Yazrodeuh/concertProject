{% extends 'base.html.twig' %}

{% block title %}Band{% endblock %}

{% block body %}
    <h1>Band</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ band.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ band.name }}</td>
            </tr>
            <tr>
                <th>MembersNumber</th>
                <td>{{ band.membersNumber }}</td>
            </tr>
            <tr>
                <th>Style</th>
                <td>{{ band.style }}</td>
            </tr>
            <tr>
                <th>UrlName</th>
                <td>{{ band.urlName }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('band_controller2_index') }}">back to list</a>

    <a href="{{ path('band_controller2_edit', {'id': band.id}) }}">edit</a> #}

    {{ include('band_controller2/_delete_form.html.twig') }}
{% endblock %}
